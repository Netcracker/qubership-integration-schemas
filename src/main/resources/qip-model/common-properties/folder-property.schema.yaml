---
$id: http://qubership.org/schemas/product/qip/common-properties/folder-property.schema.yaml
$schema: http://json-schema.org/draft-07/schema
type: object
title: Entity content with Folders
properties:
  folder:
    title: Folder
    $ref: "#/definitions/Folder"
definitions:
  Folder:
    $id: "#/definitions/Folder"
    type: object
    title: Folder
    properties:
      name:
        type: string
        title: Folder name
      subfolder:
        $ref: "#/definitions/Folder"
    required:
      - name
