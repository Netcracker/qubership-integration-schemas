---
$id: http://qubership.org/schemas/product/qip/element/header-modification.schema.yaml
$schema: http://json-schema.org/draft-07/schema
type: object
allOf:
  - $ref: "http://qubership.org/schemas/product/qip/element/properties/common-element-properties.schema.yaml"
title: Header Modification
properties:
  type:
    type: string
    const: header-modification
  properties:
    type: object
    properties:
      headerModificationToAdd:
        title: Add/Keep Headers
        description: >
          List of header names and values which we need to add or keep.
        $ref: "http://qubership.org/schemas/product/qip/element/properties/headers.schema.yaml"
      headerModificationToRemove:
        type: array
        title: Remove Headers
        description: List of header names which we need to remove.
        items:
          type: string
    additionalProperties: false
required:
  - type
